services:
  web:
    build:
      context: .
    restart: "no"
    environment:
      HOME: /app/.home
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: >
      bash -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
